apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

helmGlobals:
  chartHome: ../../charts

helmCharts:
  - name: demo-app
    releaseName: demo-dev
    namespace: dev
    valuesFile: values.yaml
